infos:
  app_name: 'ido'

models:
  user:
    name: string
    email: string
    phone_number: string
    wedding_date: date
    phone_number: string
    friend: has_many
  friend:
    user: belongs_to
    friend: belongs_to => user
  preference:
    user: belongs_to
    bidget: float
