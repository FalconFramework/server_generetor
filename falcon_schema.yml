infos:
  app_name: 'desenho'

models:
  user:
    name: string
    email: string
    age: integer
    post: has_many
    aluno: has_one

  post:
    name: string
    price: float
    user: belongs_to
  aluno:
    matricula: string
    user: references
